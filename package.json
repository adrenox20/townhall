{
  "name": "university-issue-tracker",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix worker && npm install --prefix frontend",
    "dev": "npm run dev --prefix worker",
    "dev:worker": "npm run dev --prefix worker",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix worker && npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix worker && npm run typecheck --prefix frontend"
  }
}
